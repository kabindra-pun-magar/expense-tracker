PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/expense_tracker_db
JWT_SECRET=replace_with_a_strong_random_secret_here
JWT_EXPIRES_IN=7d

# SMTP optionally used for reset emails. If not provided, code uses Ethereal test account and prints preview link.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Frontend public URL for reset links
FRONTEND_URL=http://localhost:3000
